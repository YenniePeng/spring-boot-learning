spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/data_jpa}
    username: ${DB_USERNAME:yennie}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: ${DDL_AUTO:update}
    database: postgresql
    show-sql: true
